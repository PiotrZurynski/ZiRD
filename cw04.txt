#1
CREATE VIEW v_wysokie_pensje AS
SELECT *
FROM EMPLOYEES
WHERE SALARY > 6000;

#2
CREATE OR REPLACE VIEW v_wysokie_pensje AS
SELECT *
FROM EMPLOYEES
WHERE SALARY > 12000;

#3
DROP VIEW v_wysokie_pensje;

#4
CREATE OR REPLACE VIEW v_finance AS
SELECT e.EMPLOYEE_ID, e.LAST_NAME, e.FIRST_NAME
FROM EMPLOYEES e
JOIN DEPARTMENTS d ON e.DEPARTMENT_ID = d.DEPARTMENT_ID
WHERE d.DEPARTMENT_NAME = 'Finance';

#5
CREATE OR REPLACE VIEW v_srednie_pensje AS
SELECT EMPLOYEE_ID, LAST_NAME, FIRST_NAME, SALARY, JOB_ID, EMAIL, HIRE_DATE
FROM EMPLOYEES
WHERE SALARY BETWEEN 5000 AND 12000;

#6
INSERT INTO v_srednie_pensje (EMPLOYEE_ID, LAST_NAME, FIRST_NAME, SALARY, JOB_ID, EMAIL, HIRE_DATE)
VALUES (300, 'Kowalski', 'Jan', 7000, 'IT_PROG', 'JKOWALSKI', DATE '2024-01-01');

UPDATE v_srednie_pensje
SET SALARY = 8000
WHERE EMPLOYEE_ID = 300;

DELETE FROM v_srednie_pensje
WHERE EMPLOYEE_ID = 300;


#7
CREATE OR REPLACE VIEW v_dzialy_statystyki AS
SELECT d.DEPARTMENT_ID,d.DEPARTMENT_NAME, COUNT(e.EMPLOYEE_ID)  AS LICZBA_PRACOWNIKOW,AVG(e.SALARY) AS SREDNIA_PENSJA,MAX(e.SALARY) AS MAX_PENSJA
FROM EMPLOYEES e
JOIN DEPARTMENTS d ON e.DEPARTMENT_ID = d.DEPARTMENT_ID
GROUP BY d.DEPARTMENT_ID, d.DEPARTMENT_NAME
HAVING COUNT(e.EMPLOYEE_ID) >= 4;

INSERT INTO v_dzialy_statystyki (DEPARTMENT_ID, DEPARTMENT_NAME)
VALUES (999, 'Test');



#8
CREATE OR REPLACE VIEW v_srednie_pensje_check AS
SELECT EMPLOYEE_ID, LAST_NAME, FIRST_NAME, SALARY, JOB_ID, EMAIL, HIRE_DATE
FROM EMPLOYEES
WHERE SALARY BETWEEN 5000 AND 12000
WITH CHECK OPTION;

INSERT INTO v_srednie_pensje_check (EMPLOYEE_ID, LAST_NAME, FIRST_NAME, SALARY, JOB_ID, EMAIL, HIRE_DATE)
VALUES (301, 'Nowak', 'Anna', 9000, 'IT_PROG', 'ANOWAK', DATE '2024-01-01');

INSERT INTO v_srednie_pensje_check (EMPLOYEE_ID, LAST_NAME, FIRST_NAME, SALARY, JOB_ID, EMAIL, HIRE_DATE)
VALUES (302, 'Wiśniewski', 'Piotr', 15000, 'IT_PROG', 'PWISNIEWSKI', DATE '2024-01-01');

#9
CREATE MATERIALIZED VIEW v_managerowie AS
SELECT
    e.EMPLOYEE_ID,
    e.FIRST_NAME,
    e.LAST_NAME,
    d.DEPARTMENT_NAME
FROM EMPLOYEES e
JOIN DEPARTMENTS d ON e.EMPLOYEE_ID = d.MANAGER_ID;

#10
CREATE OR REPLACE VIEW v_najlepiej_oplacani AS
SELECT *
FROM (
    SELECT EMPLOYEE_ID, FIRST_NAME, LAST_NAME, SALARY
    FROM EMPLOYEES
    ORDER BY SALARY DESC
)
WHERE ROWNUM <= 10;